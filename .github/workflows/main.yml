name: 每日更新匯率資料

on:
  schedule:
    # ── 早班：台北 07:40（UTC 23:40）每天 ──────────────────────────
    # 此時已可取得：前一交易日的 FRED 美債殖利率（台北 04:15/05:15 發布）、
    # 週六則含 CFTC 週報（台北 03:30/04:30 發布，對應頁面檢查表第 ③ 項）。
    # 刻意避開整點：GitHub 排程在 :00 最壅塞，延遲可達 20 分鐘以上。
    # 每日更新的「決策觀點」Routine 排在台北 08:15，讀的就是這一輪的產出。
    - cron: '40 23 * * *'
    # ── 晚班：台北 17:30（UTC 09:30）週一到週五 ─────────────────────
    # 台北 16:00 收盤後，錨點收盤價、台銀最終牌告、日本 JGB（16:30）皆已定案。
    - cron: '30 9 * * 1-5'
  workflow_dispatch:        # 手動觸發（Actions 頁面 Run workflow）

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: update
  cancel-in-progress: false

jobs:
  update:
    # 台銀／證交所／央行會擋境外 IP，最理想是位於台灣的 self-hosted runner。
    # 預設用 GitHub 託管的 ubuntu-latest，確保沒有 self-hosted runner 時也能跑：
    # 其餘來源全部正常，只有台銀牌告會改用「中價 × 上次加價率」推估，頁面會標示。
    # 若已架好台灣的 self-hosted runner，於 repo Variables 設定 RUNNER_LABEL=self-hosted 即可切換。
    runs-on: ${{ vars.RUNNER_LABEL || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: 安裝套件
        run: pip install -r requirements.txt

      - name: 抓取資料
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          JPY_TARGET: ${{ vars.JPY_TARGET || '84000000' }}
        run: python scripts/fetch_data.py

      - name: 檢查產出
        run: |
          python - <<'PY'
          import json,sys,datetime
          d=json.load(open('data/latest.json',encoding='utf-8'))
          assert d.get('anchor'), '缺 anchor'
          assert d.get('cftc',{}).get('net') is not None, '缺 CFTC'
          assert d.get('conf'), '缺 conformal 區間'
          age=(datetime.date.today()-datetime.date.fromisoformat(d['anchor_date'])).days
          print(f"錨點 {d['anchor_date']}（{age} 天前）· 牌告 {d['board']['spot_sell']} · 警示 {len(d.get('warnings',[]))} 則")
          if age>7: print('::warning::匯率資料超過 7 天未更新')
          for w in d.get('warnings',[]): print(f'::warning::{w}')
          PY

      - name: 提交
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/
          git diff --staged --quiet && echo "無變動" && exit 0
          git commit -m "資料更新 $(date -u +'%Y-%m-%d %H:%M') UTC"
          # 連續重跑時，別次可能已先推送 main → 先 rebase 再推、失敗重試，避免 non-fast-forward
          for i in 1 2 3; do
            git pull --rebase origin main && git push && exit 0
            echo "推送衝突，第 $i 次重試…"; sleep 5
          done
          echo "推送重試多次仍失敗"; exit 1

  deploy:
    needs: update
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.dep.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with: { path: '.' }
      - id: dep
        uses: actions/deploy-pages@v4
